# 外网访问配置指南

## 概述
本指南帮助你通过内网穿透技术，让外网用户访问部署在本地的前后端系统。

---

## 已完成配置

### ✅ 后端配置
1. **服务器监听地址**：已配置 `server.address: 0.0.0.0`（允许外部访问）
2. **跨域配置**：已添加 CORS 支持，允许所有来源访问
3. **端口**：8080

### ⚠️ 需要手动完成的配置
由于前端项目不在当前工作区，以下配置需要你手动完成。

---

## 方案选择

### 推荐方案对比

| 方案 | 优点 | 缺点 | 适用场景 |
|------|------|------|----------|
| **cpolar** | 国内速度快、中文界面 | 免费版域名不固定 | 国内用户访问 |
| **ngrok** | 国际通用、稳定 | 国内速度较慢 | 国际用户访问 |
| **花生壳** | 稳定、有免费额度 | 需要实名认证 | 长期稳定使用 |

---

## 方案一：使用 cpolar（推荐）

### 步骤1：下载安装
1. 访问官网：https://www.cpolar.com/
2. 注册账号
3. 下载 Windows 客户端并安装
4. 启动客户端并登录

### 步骤2：创建后端隧道
1. 打开浏览器访问：http://127.0.0.1:9200（cpolar管理界面）
2. 点击"隧道管理" → "创建隧道"
3. 填写配置：
   ```
   隧道名称：tlias-backend
   协议：http
   本地地址：8080
   域名类型：免费随机域名（或付费固定域名）
   ```
4. 点击"创建"
5. **复制生成的公网地址**，例如：`http://abc123.cpolar.cn`

### 步骤3：创建前端隧道（如果需要）
如果你的前端独立运行（不是通过Nginx），也需要创建隧道：
```
隧道名称：tlias-frontend
协议：http
本地地址：9528（或你的前端端口）
域名类型：免费随机域名
```

### 步骤4：修改前端配置

#### 文件位置
`D:\IDEA2025.3\IdeaProjects\vue-admin-template-master\.env.production`

#### 修改内容
```env
# just a flag
ENV = 'production'

# base api
VUE_APP_BASE_API = '/api'

# 后端API地址（替换为你的cpolar地址）
VUE_APP_URL='http://abc123.cpolar.cn/'
```

**注意**：将 `abc123.cpolar.cn` 替换为你实际获得的 cpolar 域名。

### 步骤5：重新构建前端
```bash
cd D:\IDEA2025.3\IdeaProjects\vue-admin-template-master
npm run build:prod
```

### 步骤6：部署前端到 Nginx（如果使用）
1. 将 `dist` 文件夹中的内容复制到 Nginx 的 html 目录
2. 如果使用 Nginx，确保 Nginx 也在运行

### 步骤7：测试访问
在外网浏览器访问：
- 前端：cpolar 提供的前端地址（或直接访问你的IP+端口）
- 后端API：`http://abc123.cpolar.cn`

---

## 方案二：使用 ngrok

### 步骤1：下载安装
1. 访问：https://ngrok.com/
2. 注册账号
3. 下载 Windows 版本
4. 解压到目录，如：`D:\ngrok`

### 步骤2：认证
```powershell
cd D:\ngrok
.\ngrok.exe authtoken 你的token
```
（token在 ngrok 官网 Dashboard 中获取）

### 步骤3：启动后端隧道
```powershell
.\ngrok.exe http 8080
```

记录输出的公网地址，如：`https://abc123.ngrok.io`

### 步骤4：修改前端配置
修改 `.env.production`：
```env
VUE_APP_URL='https://abc123.ngrok.io/'
```

### 步骤5：重新构建前端
```bash
cd D:\IDEA2025.3\IdeaProjects\vue-admin-template-master
npm run build:prod
```

---

## 方案三：使用花生壳

### 步骤1：下载安装
1. 访问：https://hsk.oray.com/
2. 注册并完成实名认证
3. 下载并安装花生壳客户端

### 步骤2：添加映射
1. 打开花生壳客户端
2. 点击"内网穿透" → "添加映射"
3. 配置后端映射：
   ```
   应用名称：tlias-backend
   域名类型：免费域名
   应用图标：默认
   映射类型：HTTP
   外网域名：自动生成
   内网主机：127.0.0.1
   内网端口：8080
   ```

### 步骤3：修改前端配置
将花生壳提供的域名填入 `.env.production`：
```env
VUE_APP_URL='http://你的花生壳域名/'
```

### 步骤4：重新构建前端
```bash
cd D:\IDEA2025.3\IdeaProjects\vue-admin-template-master
npm run build:prod
```

---

## 常见问题

### Q1：为什么访问很慢？
**A**：内网穿透会有额外延迟，这是正常现象。免费版通常限速。

### Q2：重启后域名变了怎么办？
**A**：免费版的域名每次重启会变化。你需要：
1. 更新前端配置中的 `VUE_APP_URL`
2. 重新构建前端
3. 或者购买付费版获得固定域名

### Q3：其他人访问时提示跨域错误？
**A**：已经配置了 CORS，确保：
1. 后端已重启
2. 前端使用的是正确的公网地址

### Q4：数据库能外网访问吗？
**A**：不建议直接暴露数据库。如果必须：
1. 在 MySQL 中创建远程访问用户
2. 防火墙开放 3306 端口
3. 修改 `application.yml` 中的数据库地址

### Q5：如何保证服务一直运行？
**A**：
1. 使用 cpolar/ngrok 的后台运行模式
2. 设置开机自启动
3. 或使用云服务器部署（最稳定）

---

## 安全检查清单

在对外开放前，请确认：

- [ ] JWT 密钥足够复杂（检查 `JwtUtils.java`）
- [ ] 数据库密码强度高
- [ ] 管理员账户密码已修改
- [ ] 防火墙只开放必要端口
- [ ] 定期查看访问日志
- [ ] 备份重要数据

---

## 快速启动脚本

### 启动后端 + cpolar（PowerShell）

创建文件 `start-all.ps1`：
```powershell
# 启动后端
Start-Process "java" -ArgumentList "-jar", "target\tlias-web-management-0.0.1-SNAPSHOT.jar" -WorkingDirectory "D:\IDEA2025.3\IdeaProjects\tlias-web-management"

# 等待3秒
Start-Sleep -Seconds 3

# 启动cpolar（假设已配置好隧道）
Start-Process "cpolar" -ArgumentList "start", "tlias-backend"

Write-Host "服务已启动！" -ForegroundColor Green
```

---

## 技术支持

如遇到问题，检查：
1. 后端日志：查看控制台输出
2. cpolar/ngrok 状态：确认隧道在线
3. 浏览器控制台：查看网络请求错误
4. 防火墙设置：确认端口已开放

---

**最后更新**：2026-05-17
